<!DOCTYPE html>
<html>
  <head>
    <title>Multi-Tenant One Db</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <section class="section">
      <div class="container">
        <!-- Main container -->
        <nav class="level">
          <!-- Left side -->
          <div class="level-left">
            <div class="level-item">
              <h3 class="title is-4 has-text-semi-bold">Multi tenant One Database App</h3>
            </div>
          </div>

          <!-- Right side -->
          <div class="level-right">
            <p class="level-item"><%= link_to 'Articles', articles_path, class: 'is-underlined' %></p>
            <p class="level-item"><%= link_to 'Authors', authors_path, class: 'is-underlined' %></p>
          </div>
        </nav>
        <hr>
        <%= yield %>
      </div>
    </section>
  </body>
</html>
